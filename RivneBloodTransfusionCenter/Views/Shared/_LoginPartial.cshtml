@using Microsoft.AspNetCore.Identity
@*@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
*@
<ul class="navbar-nav">
@*@if (SignInManager.IsSignedIn(User))
{*@
    <li class="nav-item">
        <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Привіт, @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button type="submit" class="nav-link btn btn-link text-dark">Вийти</button>
        </form>
    </li>
@*}*@
else
{
        <nav id="navbar" class="navbar">
            <ul>
@*                 <li>
                    <a asp-area="Identity" asp-page="/Account/Register">Register</a>
                </li> *@
                <li>
                    <a asp-area="Identity" asp-page="/Account/Login">Кабінет донора</a>
                </li>
            </ul>
        </nav>
}
</ul>
